### Variables
#
# Logo key. Use Mod1 for Alt and Mod4 for Super.
set $mod Mod4
set $alt_mod Mod1

# Direction keys
set $left Left
set $down Down
set $up Up
set $right Right

# Alternative direction keys
set $left_alt h
set $down_alt j
set $up_alt k
set $right_alt l

# statusbar command
set $statusbar waybar
set $waybar_position top
set $waybar_start 'killall -q waybar; sleep 0.1; waybar'

# workspace names
set $ws1 number 1
set $ws2 number 2
set $ws3 number 3
set $ws4 number 4
set $ws5 number 5
set $ws6 number 6
set $ws7 number 7
set $ws8 number 8
set $ws9 number 9
set $ws10 number 10

# Add --to-code to bindsym, support for non-latin layouts
set $bindsym bindsym --to-code

# For user's convenience, the same for unbindsym
set $unbindsym unbindsym --to-code

# background
set $background $HOME/.config/sway/images/black.png

# once-script
set $once $HOME/.config/sway/scripts/once.sh

# Your preferred apps
set $browser qutebrowser
set $browser_id org.qutebrowser.qutebrowser
set $camera guvcview
set $editor nvim

# Your preferred terminal emulator
set $term foot
set $term_cwd $term -D "$(swaycwd 2>/dev/null || echo $HOME)"
set $term_float footclient --app-id floating_shell --window-size-chars 200x60
set $term_float_once $once $term_float
set $float_editor $term_float $editor

set $float_bluetooth $term_float_once bluetui
set $float_pulsemixer $term_float_once pulsemixer
set $float_calendar $term_float_once calcurse
set $float_btop $term_float_once btop
set $float_ranger $term_float_once ranger
set $float_ncdu $term_float_once ncdu --color=dark -x ~/
set $float_htop $term_float_once htop
set $float_nmtui $term_float_once nmtui connect
set $float_sensors $term_float_once watch sensors
set $float_pacman $term_float_once 'sudo pacman -Syu'
set $float_gh_dash $term_float_once gh dash
set $float_keys $term_float_once $editor $HOME/.config/sway/../../docs/keybindings.md

set $edit_config $float_editor -O $HOME/.config/sway/config.d/definitions.conf $HOME/.config/sway/config.d/modes/default.conf

# brightness control
set $brightness_up swayosd-client --brightness raise
set $brightness_down swayosd-client --brightness lower

# scaling
set $scale_up $HOME/.config/sway/scripts/scale.sh up
set $scale_down $HOME/.config/sway/scripts/scale.sh down

# audio control
set $volume_up swayosd-client --output-volume raise
set $volume_down swayosd-client --output-volume lower
set $volume_mute swayosd-client --output-volume mute-toggle
set $mic_mute swayosd-client --input-volume mute-toggle

# clipboard history
set $clipboard rofi -modi "clipboard:$HOME/.config/sway/scripts/cliphist-rofi-img.sh" -show clipboard -show-icons -font "$gui-font" -p "Select item to copy" -lines 10 -width 35
set $clipboard_img env CLIPHIST_ROFI_GALLERY=1 rofi -modi "clipboard-img:$HOME/.config/sway/scripts/cliphist-rofi-img.sh" -show clipboard-img -show-icons -font "$gui-font" -p "Clipboard images" -lines 3 -width 90 -theme-str 'listview { columns: 5; lines: 3; spacing: 12px; fixed-columns: false; flow: horizontal; } element { orientation: vertical; padding: 8px; border: 0px; children: [element-icon]; } element-icon { size: 96px; horizontal-align: 0.5; vertical-align: 0.5; }'
set $clipboard_del cliphist list | rofi -dmenu -display-columns 2 -font "$gui-font" -p "Select item to delete" -lines 10 -width 35 | cliphist delete

# Your preferred application launcher
set $menu rofi -show combi -combi-modi "drun,run" -terminal $term -ssh-command "{terminal} {ssh-client} {host} [-p {port}]" -run-shell-command "{terminal} {cmd}" -show-icons -font "$gui-font" -lines 10 -width 35

# Lockscreen configuration
set $locking swaylock --daemonize --color 000000 --inside-color 000000 --inside-clear-color "$text-color" --ring-color "$color2" --ring-clear-color "$color11" --ring-ver-color "$color13" --show-failed-attempts --ignore-empty-password

# Idle configuration This will lock your screen after 300 seconds of inactivity, then turn off
set $idle_timeout 240
set $locking_timeout 300
set $screen_timeout 600
set $sleep_timeout 900
set $sleep_delay 2

set $swayidle swayidle -w \
    timeout $idle_timeout 'light -G > /tmp/brightness && light -S 10' resume 'light -S $([ -f /tmp/brightness ] && cat /tmp/brightness || echo 100%)' \
    timeout $locking_timeout 'exec $locking' \
    timeout $screen_timeout 'swaymsg "output * dpms off"' \
    resume 'swaymsg "output * dpms on"' \
    timeout $sleep_timeout 'swaymsg "output * dpms on"; sleep $sleep_delay; systemctl suspend' \
    before-sleep 'playerctl pause' \
    before-sleep 'exec $locking & sleep $sleep_delay'

# screenshot
set $grimshot grimshot
set $screenshot_dir "$HOME/Pictures/Screenshots"
set $screenshot_file "$screenshot_dir/grimshot-$(date +'%Y-%m-%d_%H-%M-%S.png')"
set $notify_paste [[ $(wl-paste -l) == "image/png" ]] && notify-send "Screenshot copied to clipboard"
set $swappy swappy -f -
set $swappy_pipe swappy -f - -o -
set $upload_pipe curl -s -F "file=@-;filename=.png" https://x0.at/ | tee >(wl-copy) >(xargs notify-send)

set $screenshot_copy_area $grimshot copy anything && $notify_paste
set $screenshot_copy_save_area $grimshot savecopy anything "$screenshot_file" && $notify_paste
set $screenshot_edit_area $grimshot save anything - | $swappy
set $screenshot_save_screen $grimshot save output "$screenshot_file"
set $screenshot_upload_area $grimshot save anything - | $swappy_pipe | $upload_pipe

set $recorder $HOME/.config/sway/scripts/recorder.sh
set $color_picker $once grimpicker -n -c

# autostarts
set $autostart_dex '[ -x "$(command -v dex)" ] && gdbus wait --session org.kde.StatusNotifierWatcher && dex --autostart'
set $wlsunset '[ -x "$(command -v wlsunset)" ] && $HOME/.config/sway/scripts/sunset.sh "on"'
set $autotiling '[ -x "$(command -v autotiling)" ] && autotiling'
set $foot_server '[ -x "$(command -v foot)" ] && foot --server'
set $mounting_daemon '[ -x "$(command -v pcmanfm)" ] && pcmanfm -d'
# restart kanshi https://github.com/emersion/kanshi/issues/43#issuecomment-531679213
set $kanshi '[ -x "$(command -v kanshi)" ] && pkill kanshi; exec kanshi'
set $xdg-dirs '[ -x "$(command -v xdg-user-dirs-update)" ] && exec xdg-user-dirs-update'
# apply the keyboard layout from localectl if no keyboard layout has been set via config.d
set $auto_xdg_keyboard 'grep -q xkb_layout ~/.config/sway/config.d/*.conf || $HOME/.config/sway/scripts/keyboard.sh'

# daemons
set $swaync '[ -x "$(command -v swaync)" ] && swaync'
set $swayosd '[ -x "$(command -v swayosd-server)" ] && swayosd-server'
set $poweralert '[ -x "$(command -v poweralertd)" ] && pkill poweralertd; poweralertd -s -i "line power" &'
set $cliphist_watch '[ -x "$(command -v wl-paste)" ] && [ -x "$(command -v cliphist)" ] && wl-paste --watch pkill -RTMIN+9 waybar'
set $cliphist_store_text '[ -x "$(command -v wl-paste)" ] && [ -x "$(command -v cliphist)" ] && wl-paste --type text --watch cliphist store'
set $cliphist_store_image '[ -x "$(command -v wl-paste)" ] && [ -x "$(command -v cliphist)" ] && wl-paste --type image --watch cliphist store'
set $calendar_daemon 'calcurse --daemon'
set $dropbox_daemon '[ -x "$(command -v dropbox)" ] && dropbox start'
set $watch_playerctl '[ -x "$(command -v playerctl)" ] && pkill playerctl; playerctl -a metadata --format \'{{status}} {{title}}\' --follow | while read line; do pkill -RTMIN+5 waybar; done'
